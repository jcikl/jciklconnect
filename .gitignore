# Dependencies
node_modules/

# Service account credentials — never commit
scripts/serviceAccount.json
**/serviceAccount.json
serviceAccountKey.json

# Generated schema / introspection outputs
firestore-schema.json
extractFirestoreSchema.cjs

# Android signing — never commit
android/jcikl-release.jks
android/app/google-services.json
/.pnp
.pnp.js

# Testing
/coverage

# Production
/dist

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local migration/audit reports and script backups
*.bak-*
*-report.json
lark-migration-inventory.json
.lark-jcikl-state.json

# IDEs and editors
.idea/
.vscode/
.claude/*
!.claude/launch.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Project-specific caches/directories
.codegraph/
.understand-anything/
.cursor/
tsc_errors_latest.txt
tsc_errors_new.txt

# Local Netlify folder
.netlify

# Capacitor native platforms (generated — do not commit secrets inside)
# android/ and ios/ are committed so CI can build them,
# but exclude generated/build artefacts inside them
android/.gradle/
android/app/build/
android/build/
ios/App/Pods/
ios/App/App.xcworkspace/xcuserdata/
*.ipa
*.apk
*.aab
